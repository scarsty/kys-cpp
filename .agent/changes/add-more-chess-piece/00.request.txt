Our game has limited amount of usable chess pieces, they are currently defined in 2 places

- the base stats in the database in 0.db, see .claude\r-database-instruction.md
- the chess configurations under config/chess_*, this defines the active characters we use in database and their additional synergy/combo effects

We want to expand to a larger pool of them and rework some of the synergy effects. 

See high level design of characters we want to add in .agent\changes\add-more-chess-piece\倚天+飞狐+部分补完第二版.md

Please confine your work to just plan for those characters.

When adding a character we need to
- make sure its stats in 0.db matches its tier/cost, you can gauge the general strength/range by looking at the current pool
- we need to select skills that fit the character
    - generally 1 normal skill (lower damaged) and 1 ultimate (higher damaged)
    - the .md doc have it currently specified but we can change if you see fit 
    - the operation type of the skill (eg. range or melee) is best to stay consistent 
    - or at least not to have a melee ultimate when the normal attack is ranged
    - but anything else is acceptable
- if the character is brand new (does not currently exist in 0.db) or lacks fight frames we need to add one
- this is a mod of a different game D:\games\人在江湖cpp版\人在江湖cpp版\game\save\CSV - it has some data for reference
    - most importantly, it has fight frame images/data where we can copy from D:\games\人在江湖cpp版\人在江湖cpp版\game\resource\fight
    - and head resource D:\games\人在江湖cpp版\人在江湖cpp版\game\resource\head
    - those are used for display in game and each character has to have one
    - if the character (by name) doesn't exist in 人在江湖 either - then pick a character data that fits the description from 人在江湖
    - again note the ids between games are not shared, you need to match by name, and then find the head id and fight frame id

If a skill/magic doesn't exist, you can create one in the 0.db, give it stats that you see fit
    - not all stats are used in game/battle
    - pick a good eft, you can find them in work\game-dev\resource\eft

To make the changes you make more transparent
    - Write a python script (or sql insert/update queries) that can be audited to make the database changes
    - This also allows us to tune the numbers and rerun if needed

We will need to reconfigure some synergies as shown in the new doc and some additional battle effects may need to be implemented in code.
    - make sure to correctly add trigger effects if needed
    - the new battle effects may not be clearly described, I will need you to imaginate and implement something that makes sense and make sure it feels unique
    - the stat numbers may also be a bit off, tune it if it needs to be done
    - when implementing a synergy, make sure they can stack in some form or way if it makes sense
        - additive, multiplicative, adding duration, or take max of whatever value
        - avoid simple overrides

Adding characters to the pool means balances are all off
    - the game is in a decently balanced state right now
    - new characters means the chess shop will have a larger pool, and there is less chance to roll a specific chess
    - the tiering balance / probablity will also be off
    - we need strategies to rebalance the pool, we need to evaluate multiple strategies
    - expanding the shop to 6 per roll, make this configurable
    - adding chess bans (with a fixed number of times) that the player can choose after it is being seen in the shop
        - banning will be in a separate chess menu, make it clear it has to be seen once before getting banned in that menu
        - needs to be saved and persisted
        - number of total bans needs to be configurable, and do some calculation to tell me how many we should hand out
    - all of this can be confusing to new player
        - in easy mode we should keep a good set of pool of initial characters, so the player doesn't need to know about these complications
        - however without the neccessary pieces, the synergy combos may not even have the minimum required pieces
        - which continues to be confusing, so we need to pick a good set 
            - where each synergy would either not be shown at all (no chess chosen has that synergy)
            - or is guranteed to meet the max threshold
            - keep the distribution to be similar to what we have right know, so the shop roll is still 5
    - in the future we may 
        - have a "seasonal" approach where we rotate the pool (as we add new character/chess)
        - let player select novel add-on, where additional pieces from certain 金庸 novels are added into the pool, as you can see this is mostly just for 倚天屠龙记
        - You don't need to implement these 2 points but design with those in mind, as this maybe a temporary solution
