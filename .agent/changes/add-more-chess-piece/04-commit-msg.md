feat(chess): expand pool to 88 characters with 13 new synergies

Grow the auto-battler roster with dozens of new martial heroes, reworked synergy trees, and
new combat mechanics that make positioning, deaths, shields, dashes, and combo timing matter
more from shop roll to boss fight.

- Normal mode now rolls 6 shop slots and supports run-based `禁棋` filtering after a piece is seen
- Easy mode uses its own curated pool with complete synergy breakpoints and no ban management
- New synergies like `刀客`, `真武七截阵`, `妙手仁心`, and `四大法王` unlock fresh build paths
- Classic synergies such as `剑客`, `拳师`, `红颜`, and `全真教` now have broader endgame caps
- New battle effects add executes, bleeds, clone summons, dashes, force pulls, and shield payoffs

Example:
`刀客` can now reach its full six-member capstone instead of stopping at the old roster limits.
